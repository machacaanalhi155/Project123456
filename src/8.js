import { Project123456 } from '../../../Project123456';

const generateRandomCode = () => {
  const result = Math.floor(Math.random() * 1000);
  return result;
};

export default generateRandomCode();
